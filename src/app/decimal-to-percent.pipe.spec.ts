import { DecimalToPercentPipe } from './decimal-to-percent.pipe';

describe('DecimalToPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalToPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
